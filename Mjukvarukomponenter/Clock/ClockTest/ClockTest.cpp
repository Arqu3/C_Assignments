#include "ClockTest.h"
#include "Clock.h"
#include <cassert>

using namespace std;

void ClockTest::run()
{
	Clock c0;
	//assert(c0.getSecond() == 0);
	//assert(c0.getMinute() == 0);
	//assert(c0.getHour() == 0);
	//assert(c0.getDay() == 0);

}